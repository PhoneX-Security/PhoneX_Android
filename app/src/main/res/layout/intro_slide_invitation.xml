<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#333333"
    android:gravity="center_horizontal"
    tools:context=".SliderActivity$PlaceholderFragment">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:scaleType="fitCenter"
        android:src="@drawable/svg_phonex_logo"
        android:layout_above="@+id/image2"
        android:id="@+id/image" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:scaleType="fitCenter"
        android:layout_centerInParent="true"
        android:src="@drawable/phx_text_logo"
        android:layout_marginTop="16dp"
        android:id="@+id/image2" />

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#fff"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/image2"
        android:textSize="22sp"
        android:text="@string/secure_communication"  />

    <Button
        android:id="@+id/get_started_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        style="@style/SlideButtonFlatOrange"
        android:layout_below="@+id/title"
        android:layout_marginTop="60dp"
        android:text="@string/get_started" />

    <TextView
        android:id="@+id/text_terms_of_use"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="@color/phonex_white"
        android:textSize="14sp"
        android:fontFamily="sans-serif-light"
        android:text="@string/link_terms_of_use"
        />

</RelativeLayout>
